// Problem Statement Simple JSX Element Objective: Create a paragraph (

// ) element using JSX to display the text "Seize the day". Requirements: Use the provided boilerplate HTML code. Inside the

let element = <p>Seize the day</p>
// Rendering the React element into the root DOM node
ReactDOM.render(element, document.getElementById('root'))